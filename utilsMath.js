export const suma = (a, b) => {
	return a + b;
}; //Modulo

export const resta = (a, b) => {
	return a - b;
}; //Modulo

export const multiplicacion = (a, b) => {
	return a * b;
}; //Modulo

// module.exports = {
// 	suma,
// 	resta,
// 	multiplicacion,
// };
